# mealog - 스마트 식사 타임라인

## 📁 파일 구조

```
mealog/
├── index.html              # 메인 HTML 파일
├── css/
│   └── style.css          # 스타일시트
├── js/
│   └── main.js            # JavaScript 코드
├── server.ps1             # PowerShell 웹 서버 스크립트
├── start.bat              # 서버 시작 배치 파일
└── README.md              # 이 파일
```

## 🚀 다른 컴퓨터에서 실행하기

### 필요한 파일들
다음 파일들을 모두 복사해야 합니다:
- `index.html`
- `css/style.css`
- `js/main.js`
- `server.ps1` (또는 `start.bat`)

### 실행 방법

**Windows에서:**
1. 모든 파일을 같은 폴더 구조로 복사
2. `start.bat` 파일 더블클릭
3. 브라우저에서 `http://localhost:8000` 접속

**또는 PowerShell에서:**
```powershell
cd [파일이 있는 폴더]
.\server.ps1
```

## 📱 스마트폰에서 접속하기

### 준비사항
1. **같은 Wi-Fi 네트워크**: 스마트폰과 컴퓨터가 같은 Wi-Fi에 연결되어 있어야 합니다
2. **서버 실행**: `start.bat`를 실행하여 서버를 시작합니다
3. **IP 주소 확인**: 서버 시작 시 화면에 표시되는 IP 주소를 확인합니다

### 접속 방법
1. 서버를 시작하면 화면에 다음과 같이 표시됩니다:
   ```
   컴퓨터에서 접속:
     http://localhost:8000
   
   스마트폰에서 접속 (같은 Wi-Fi 필요):
     http://192.168.x.x:8000
   ```

2. 스마트폰 브라우저에서 위의 IP 주소를 입력합니다
   - 예: `http://192.168.133.71:8000`

### 문제 해결

**스마트폰에서 접속이 안 될 때:**
1. **같은 Wi-Fi 확인**: 스마트폰과 컴퓨터가 같은 네트워크인지 확인
2. **방화벽 확인**: Windows 방화벽이 포트 8000을 차단할 수 있습니다
   - Windows 설정 > 방화벽 > 고급 설정
   - 인바운드 규칙 추가: 포트 8000 허용
3. **IP 주소 확인**: 서버 시작 화면에 표시된 IP 주소가 맞는지 확인
4. **관리자 권한**: PowerShell을 관리자 권한으로 실행해보세요

## ⚠️ 주의사항

1. **인터넷 연결 필요**
   - Tailwind CSS, Font Awesome, Chart.js는 CDN에서 로드됩니다
   - Firebase도 인터넷 연결이 필요합니다

2. **Windows PowerShell 필요**
   - `server.ps1`은 Windows PowerShell에서만 작동합니다
   - 다른 OS에서는 Python이나 Node.js 웹 서버를 사용하세요

3. **Firebase 설정**
   - Firebase 설정은 이미 코드에 포함되어 있습니다
   - 다른 Firebase 프로젝트를 사용하려면 `js/main.js`의 Firebase 설정을 수정하세요

4. **포트 충돌**
   - 포트 8000이 사용 중이면 `server.ps1` 파일에서 포트 번호를 변경하세요

5. **보안**
   - 로컬 네트워크에서만 접근 가능합니다
   - 인터넷에 공개하려면 별도의 보안 설정이 필요합니다

## 🔧 문제 해결

**"사이트에 연결할 수 없습니다" 오류:**
- PowerShell을 관리자 권한으로 실행해보세요
- 방화벽 설정을 확인하세요
- 다른 포트(8080 등)를 사용해보세요

**JavaScript가 로드되지 않음:**
- 파일을 직접 열지 말고 웹 서버를 통해 실행하세요
- `file://` 프로토콜로는 ES6 모듈이 작동하지 않습니다

**스마트폰에서 접속 안 됨:**
- 같은 Wi-Fi 네트워크인지 확인
- 방화벽에서 포트 8000 허용
- IP 주소가 올바른지 확인

## 📦 전체 폴더 복사 방법

1. `mealog` 폴더 전체를 USB나 클라우드에 복사
2. 다른 컴퓨터에 붙여넣기
3. 폴더 구조를 그대로 유지
4. `start.bat` 실행

## ✅ 체크리스트

다른 컴퓨터에서 실행하기 전에 확인:
- [ ] 모든 파일이 복사되었는가?
- [ ] 폴더 구조가 동일한가? (css/, js/ 폴더 포함)
- [ ] 인터넷 연결이 되어 있는가?
- [ ] Windows PowerShell이 설치되어 있는가?

스마트폰에서 접속하기 전에 확인:
- [ ] 서버가 실행 중인가?
- [ ] 스마트폰과 컴퓨터가 같은 Wi-Fi에 연결되어 있는가?
- [ ] IP 주소가 올바른가?
- [ ] 방화벽 설정이 올바른가?
